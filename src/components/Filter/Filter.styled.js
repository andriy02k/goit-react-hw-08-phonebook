import styled from "styled-components";

export const Filters = styled.label(() => {
    return {
        paddingLeft: '20px',
        input: {
            display: 'block',
            margin: '4px 0 0 20px',
            borderColor: '#D3D3D3'
        }
    };
});